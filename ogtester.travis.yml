language: python
# command to install dependencies
install:
  - pip install -r requirements.txt
# command to run tests
script:
  - coverage run mockup.py
  - coverage run ScrapeBooks.py
  - coverage run StoreChars.py
